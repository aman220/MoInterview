## Main site production pass (2026-07-20)
- Created 6 missing pages (were dead `#` footer links): `/about`, `/blog` (+ `/blog/[slug]` dynamic post route backed by `lib/blog.ts` with 3 real posts), `/contact` (client form → real `mailto:` compose), `/privacy`, `/terms`, `/cookies` (shared `components/legal-doc.tsx`). All under `(main)` layout (Navbar+Footer), Tailwind v4 marketing aesthetic, real content, all HTTP 200.
- Footer: wired Company (About/Blog/Contact) + Legal (Privacy/Terms/Cookies) links; `© {new Date().getFullYear()}`; social icons → external URLs w/ aria-labels.
- Bug fixes: renamed 12 stray "InterviewHub" refs → "MoInterview" (7 files incl. landing, become-interviewer, testimonials, why-us, value-propositions, dashboard settings/feedback); fixed broken `bg-[linear-linear(...)]` grid utility → `linear-gradient`.
- Verified in-browser: /about renders, no console errors, all footer links resolve, landing shows "Why MoInterview".
- NOT done this pass (honest backlog): interviewer-dashboard dead-button/mock/missing-state sweep; landing **testimonials** remain curated editorial content (no backend "featured reviews" domain — the reviews table maps poorly to the "author, role at company" testimonial format since review authors are candidates without reliable role/company). Recommend keeping testimonials curated OR building a proper featured-testimonials domain with explicit curation.

## Blog article design implemented (2026-07-20)
- Ported the approved "Behavioral Stories That Land" reading-experience design into `/blog/[slug]`, faithful to the HTML/CSS the user supplied.
- Content model upgraded: `lib/blog.ts` now uses a typed `Block[]` (p/h2/h3/ul/ol/quote/callout/example/checklist) + `Author` object; behavioral post carries the design's full content (STAR-R, worked example, checklist). `tableOfContents()`, `wordCount()` helpers.
- New files: `app/(main)/blog/[slug]/article.module.css` (design CSS scoped to `.reader`, offsets adjusted for the site's 64px navbar, design's own nav/footer dropped in favor of the site's real auth-aware Navbar/Footer, dark tokens via `:global(html.dark) .reader`), `app/(main)/blog/[slug]/ArticleReader.tsx` (client: reading progress, left action rail, scrollspy TOC + mobile TOC, font-size A±, clap/bookmark/share, helpful votes, comments, selection→copy/tweet toolbar, back-to-top, mobile rail). Fonts: IBM Plex Sans/Serif/Mono via next/font (variables on the wrapper).
- SEO preserved & improved: BlogPosting + BreadcrumbList JSON-LD, article OpenGraph/Twitter, canonical; sitemap/robots from the earlier pass.
- Verified in-browser: IBM Plex Serif title @52px, 3-col grid `56px 636px 260px`, callout/example/checklist render, clap 1284→1285, checklist toggles, helpful 212→213, dark mode adapts (`--ai-1`→#c49060), zero console errors.
- Notes: claps/comments/helpful/bookmark are client-side (match the design; not yet persisted — a future engagement-backend could store them). Cover image is the design's placeholder pattern (swap for real per-post images later). next/font pulls Google Fonts at build; behind local Avast TLS interception it warns but succeeds — set NEXT_TURBOPACK_EXPERIMENTAL_USE_SYSTEM_TLS_CERTS=1 or self-host fonts if it ever hard-fails in CI.

## Removed dead main-site interviewer dashboard (2026-07-20)
- It wasn't literally dead — `dashboardPath()`, the interviewer-onboarding redirects (x3), and the navbar all routed interviewers to the main-site mock `/dashboard/interviewer`. Per architecture (the standalone Interviewer-MoInterview app is the real coach dashboard), repointed that routing to the coach app first, then deleted the mock.
- New: `COACH_APP_URL` in `lib/auth.ts` = `process.env.NEXT_PUBLIC_COACH_APP_URL ?? 'http://localhost:3002'`. `dashboardPath(INTERVIEWER)` now returns it (external). **Set NEXT_PUBLIC_COACH_APP_URL to the deployed coach app URL in production.** Navbar renders the Dashboard link as a plain `<a>` when the href is external. Onboarding redirects (2 `window.location.href`, 1 `<Link>`→`<a>`) now go to COACH_APP_URL.
- Deleted 18 files: `app/dashboard/interviewer/` (7 route pages) + `components/interviewer-dashboard/` (11 components, imported only by those pages). `app/dashboard/{layout,settings,feedback/[id],interview-report/[id]}` were NOT the interviewer dashboard and are kept.
- Also fixed a pre-existing `app/sitemap.ts` type error (changeFrequency widened to string → would fail `next build`).
- Verified: typecheck clean; `/dashboard/interviewer`→404; home/onboarding/find-interviewers→200; no dangling refs; no console errors.
